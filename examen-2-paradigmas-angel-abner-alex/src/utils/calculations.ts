import type { CalculationResult, SavingsFormData } from "../types";

/**
 * Función temporal para calculo de proyecciones.
 * Ángel la implementará con la lógica completa 
 */
export const calculateProjections = (
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  _formData: SavingsFormData
): CalculationResult => {
  return {
    monthlyProjections: [],
    annualProjections: [],
    totalInterest: 0,
    finalBalance: 0,
  };
};
