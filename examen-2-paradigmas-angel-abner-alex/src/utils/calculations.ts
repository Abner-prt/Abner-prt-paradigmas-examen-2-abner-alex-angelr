import { CalculationResult, SavingsFormData } from "../types";

/**
 * Función temporal para cálculo de proyecciones (Placeholder para Commit 2).
 * Ángel la implementará con la lógica completa en el Commit 4.
 */
export const calculateProjections = (
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  _formData: SavingsFormData
): CalculationResult => {
  return {
    monthlyProjections: [],
    annualProjections: [],
    totalInterest: 0,
    finalBalance: 0,
  };
};
